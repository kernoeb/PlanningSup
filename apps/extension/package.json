{
  "name": "planningsup-extension",
  "displayName": "PlanningSup",
  "type": "module",
  "version": "0.0.2",
  "private": true,
  "description": "Votre calendrier universitaire, simple, rapide, efficace",
  "scripts": {
    "dev": "bun run clear && cross-env VITE_AUTH_ENABLED=true VITE_BACKEND_URL=http://localhost:20000 NODE_ENV=development concurrently bun:dev:*",
    "dev-firefox": "bun run clear && cross-env NODE_ENV=development EXTENSION=firefox bun:dev:*",
    "dev:prepare": "bun scripts/prepare.ts",
    "dev:background": "bun run build:background -- --mode development",
    "dev:web": "bunx --bun vite",
    "build": "export NODE_ENV=production && bun run clear && bun run build:web && bun run build:prepare && bun run build:background",
    "build:prepare": "bun scripts/prepare.ts",
    "build:background": "bunx --bun vite build --config vite.config.background.ts",
    "build:web": "bunx --bun vite build",
    "pack": "cross-env NODE_ENV=production concurrently bun:pack:*",
    "pack:zip": "rimraf extension.zip && cd extension && zip -r ../extension.zip . && cd ..",
    "pack:crx": "crx pack extension -o ./extension.crx",
    "pack:xpi": "cross-env WEB_EXT_ARTIFACTS_DIR=./ web-ext build --source-dir ./extension --filename extension.xpi --overwrite-dest",
    "start:chromium": "web-ext run --source-dir ./extension --target=chromium",
    "start:firefox": "web-ext run --source-dir ./extension --target=firefox-desktop",
    "clear": "rimraf --glob extension/dist extension/manifest.json extension.*",
    "lint": "eslint --cache .",
    "lint-fix": "eslint --cache --fix .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@tailwindcss/vite": "catalog:ui",
    "@types/bun": "catalog:types",
    "@types/fs-extra": "11.0.4",
    "@types/webextension-polyfill": "0.12.4",
    "@vitejs/plugin-vue": "catalog:dev",
    "chokidar": "5.0.0",
    "cross-env": "10.1.0",
    "crx": "5.0.1",
    "daisyui": "catalog:ui",
    "fs-extra": "11.3.3",
    "rimraf": "6.1.2",
    "sass-embedded": "catalog:ui",
    "tailwindcss": "catalog:ui",
    "typescript": "catalog:dev",
    "vite": "catalog:dev",
    "vue": "catalog:ui",
    "web-ext": "9.2.0",
    "webext-bridge": "6.0.1",
    "webextension-polyfill": "0.12.0"
  }
}
