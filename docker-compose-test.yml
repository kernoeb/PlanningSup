services:
  web:
    #build: .
    image: ghcr.io/kernoeb/planningsup/planning
    restart: always
    stdin_open: true
    tty: true
    ports:
      - "31021:3000"
    networks:
      - planning
    depends_on:
      - mongodb
    env_file:
      - .env
    environment:
      - MONGODB_URL=mongodb:27017
  mongodb:
    image: mongo:7.0
    restart: always
    volumes:
      - '.mongo-planning:/data/db'
    networks:
      - planning

networks:
  planning:
    driver: bridge
